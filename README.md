# physics-hackathon

cd Game
python -m venv venv

virtual envrionment activation
For Window 
    venv\Scripts\activate
For MacOS
    .venv\bin\activate

pip install pygame
pip install radioactivedecay
pip install numpy